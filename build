#!/bin/bash

if [ "$OS" == "darwin" ]; then
	bash .scripts/brew_replicate
fi
if [ "$OS" == "linux" ]; then
	bash .scripts/emerge_replicate
fi

if [ "$EUID" -ne 0 ]; then
	cargo install $(cat .packages/cargo)
else
	exit
fi
